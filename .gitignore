# secrets — NEVER commit these
.env

# the virtual environment — it's huge and machine-specific
.venv/

# the database file — it's data, not code
*.db

# Python's compiled cache
__pycache__/
*.pyc

# editor / OS noise
.vscode/
.idea/
.DS_Store
.env.local
test.py